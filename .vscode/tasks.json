{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "parser",
            "type": "shell",
            "command": "cd ${workspaceFolder}/src/frontend/parser && make clean && make",
            "args": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}